# Observability Dashboard Configuration
# Copy this file to .env and update the values

# ===========================================
# General Settings
# ===========================================
NODE_ENV=DEV                    # DEV or PROD
DASHBOARD_PORT=5100             # Port for the dashboard server

# ===========================================
# Authentication (Initial Setup Only)
# ===========================================
# Credentials are stored in the database after first run
# These env vars are only used to create the default admin user on first startup
DEFAULT_ADMIN_PASSWORD=         # Password for default admin user (used on first run only)
DEFAULT_AUTH_TOKEN=             # Auth token for API/WebSocket (used on first run only)

# ===========================================
# Log Directory
# ===========================================
LOG_DIR=/home/ubuntu/bulwark-stack-org/logs

# ===========================================
# Development Database
# ===========================================
DEV_DB_HOST=192.168.50.90
DEV_DB_PORT=5432
DEV_DB_DATABASE=bulwark
DEV_DB_USER=postgres
DEV_DB_PASSWORD=postgres

# ===========================================
# Production Database (REQUIRED for PROD environment)
# ===========================================
PROD_DB_HOST=                   # e.g., your-prod-host.rds.amazonaws.com
PROD_DB_PORT=5432
PROD_DB_DATABASE=bulwark
PROD_DB_USER=postgres
PROD_DB_PASSWORD=               # Production database password (REQUIRED)

# ===========================================
# AWS Configuration (optional)
# ===========================================
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=ap-southeast-1
